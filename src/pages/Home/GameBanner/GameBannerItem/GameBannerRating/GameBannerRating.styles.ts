import { THEME } from 'src/shared/theme';
import styled from 'styled-components';

export const StyledGameBannerRating = styled.div`
  color: ${THEME.TEXT_PRIMARY_INVERSE};
  display: flex;
  flex-direction: column;
`;
